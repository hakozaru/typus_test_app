<%= form_for(@sample_resource) do |f| %>
  <% if @sample_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sample_resource.errors.count, "error") %> prohibited this sample_resource from being saved:</h2>

      <ul>
      <% @sample_resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
